<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_2.xsd"
             version="3.2">
    <persistence-unit name="default" transaction-type="RESOURCE_LOCAL">
        <!-- Provider Hibernate -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Các entity sẽ được Hibernate tự động scan (nếu để trống thì Hibernate tự tìm trong project) -->
        <class>iuh.fit.se.mssv22714321_phanquochuy_tuan05.entity.NhaCungCap</class>
        <class>iuh.fit.se.mssv22714321_phanquochuy_tuan05.entity.DienThoai</class>
        <!-- Thêm entity khác nếu cần -->

        <properties>
            <!-- JDBC -->
            <property name="jakarta.persistence.jdbc.driver" value="org.mariadb.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mariadb://localhost:3306/userdb"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="root"/>

            <!-- Hibernate dialect cho MariaDB -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MariaDBDialect"/>

            <!-- Auto DDL: Hibernate sẽ tự động convert Entity thành bảng -->
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>
            <!-- Các giá trị khác: none | validate | update | create | create-drop -->

            <!-- Hiện SQL ra console -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- Tắt warning pool connection -->
            <property name="hibernate.connection.pool_size" value="10"/>

            <!-- Auto scan class-->
            <property name="hibernate.archive.autodetection" value="class"/>
        </properties>
    </persistence-unit>
</persistence>
